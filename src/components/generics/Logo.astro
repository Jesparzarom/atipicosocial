---
import logo from "../../assets/logos/atipicosocial.png";
import {Image} from "astro:assets";

interface Props {
    className?:string;
    width?:number;
    height?:number;
    loading?:any;
}

const {className, width, height, loading} = Astro.props
---
<Image src={logo} alt ="Logo de Atípico Social" width={width} height={height} class={`${className}`} loading={ loading || "eager"}/>